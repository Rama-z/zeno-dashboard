[build]
  command = "npm run build"
  publish = "dist"

# Backend API proxy
[[redirects]]
  from = "/api/*"
  to = "https://api.zeno-domain-anda.com/api/:splat"
  status = 200

# SPA fallback — harus setelah API proxy
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200